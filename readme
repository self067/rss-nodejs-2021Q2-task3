Найденные ошибки компиляции
<описание ошибки>. <описание исправления>. Исправлена(ы) строка(и) <номер(а) строк> в файле <относительный путь к файлу из корневой папки> ...
1. неверно импортируется Router() . заменено на var router = require("express").Router(); .исправлена строка 1 в файле controllers/usercontroller.js
2. ошибка в имени пакета bcryptjs . исправлено bcrypt на bcryptjs. исправлена строка 2 в файле controllers/usercontroller.js
3. ошибка в импорте User. исправлено на  require("../models/user"). исправлена строка 5 в файле controllers/usercontroller.js
4. ошибка в импорте Game. исправлено на  require("../models/game"). исправлена строка 2 в файле controllers/gamecontroller.js
5. отсутствует module.exports. добавлено module.exports = . исправлена строка 1 в файле models/game.js


Найденные ошибки логики приложения
<описание ошибки>. <описание исправления>. Исправлена(ы) строка(и) <номер(а) строк> в файле <относительный путь к файлу из корневой папки> ...
Рефактор кода
<Описание, что было изменено>
